services:
  - type: web
    name: fastapi-backend
    env: python
    buildCommand: ""
    startCommand: uvicorn main:app --host=0.0.0.0 --port=10000
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: chatdb
          property: connectionString